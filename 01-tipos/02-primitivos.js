/*
Tipos de datos:
 - Primitivos
 Se guardan en una memoria llamada como Stack
 Se guardan directamente en memoria, por tanto, se podrán leer lo datos
 da manera rapida.

 - De referencia
Se guarda en una memoria llamada como Heap
Se guarda una referencia, direccion de memoria en la que estará el dato,
que permitirá acceder a la informacion. Esta lectura es mas lenta.
*/

let numero = 1;
let texto = "Hola mundo";
let verdadero = true;
let falso = false;
let noDefinido;
let undef = undefined;
let nulo = null;